# Environment example for Intelligent Process Automation

# ---------------- Server (Express) ----------------
# MongoDB connection string (replace with your DB URI)
MONGO_URI=mongodb://localhost:27017/ipa_db

# Port where the Express server runs
PORT=5005

# JWT secret for signing tokens (use a strong secret in production)
JWT_SECRET=your_jwt_secret_here

# URL to the chatbot Flask service (used by server-side code)
CHATBOT_URL=http://localhost:5002


# ---------------- Client (Next.js) ----------------
# Base API URL for the Next client to reach the Express server (include /api path if used in code)
NEXT_PUBLIC_API_URL=http://localhost:5005/api

# Chatbot URL for client-side direct calls to Flask (optional; client defaults to localhost:5002)
NEXT_PUBLIC_CHATBOT_URL=http://localhost:5002

# URL to the OCR Flask service (server-side)
OCR_URL=http://localhost:5001

# Client-side OCR URL (if the client talks directly)
NEXT_PUBLIC_OCR_URL=http://localhost:5001

# ---------------- Notes ----------------
#  - Copy this file to .env (server) and to client/.env.local (if needed) and fill values.
#  - Keep secrets out of version control. Use secure secret management for production.
